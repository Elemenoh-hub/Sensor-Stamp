<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose owns all of the in-app styling; this theme only needs to give the
      window a matching background so there is no flash on cold start.
    -->
    <style name="Theme.SensorStamp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
